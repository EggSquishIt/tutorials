
// Run this program with:
// node hello.js
// or:
// nodejs hello.js

// Calls the "log" method (which is a function) in the "console" object, passing a text string to it
// The function sends the text (and a newline) to stdout
console.log("Hello world!");

