
# Run this program with:
# bash hello.sh

# Runs the "echo" built-in command, passing text as its first command line argument
# The command sends the text (and a newline) to stdout
echo "Hello world!"

