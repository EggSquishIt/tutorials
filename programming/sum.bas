
1 rem Run this program with:
2 rem pcbasic sum.bas

3 rem Line 10 is the "input" command which asks the user for one value and places it into variable "a"
10 input a

11 rem Line 20 is the "input" command which asks the user for one value and places it into variable "b"
20 input b

21 rem Line 30 sums the numbers in the "a" and "b" variables and prints the result using the "print" command
30 print a + b

